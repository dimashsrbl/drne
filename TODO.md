# Туду-лист: Система управления автономным дроном (MVP)

По ТЗ из `tz.md`. Порядок — от инфраструктуры к UI.

---

## Фаза 0: Окружение и симуляция

- [x] **0.1** Установить и запустить **PX4 Autopilot** (SITL или сборка под симуляцию)
- [x] **0.2** Установить и запустить **Gazebo** (симулятор)
- [x] **0.3** Установить **QGroundControl** для проверки связи с PX4
- [x] **0.4** Убедиться, что PX4 ↔ Gazebo работают (взлёт в QGC)

---

## Фаза 1: Backend и Drone Control Service

- [x] **1.1** Создать проект бэкенда (**FastAPI** или **NestJS**)
- [x] **1.2** Подключить **MAVLink** (pymavlink / node-mavlink или аналог)
- [x] **1.3** Реализовать **Drone Control Service**:
  - [x] arm / disarm
  - [x] takeoff (с параметром altitude)
  - [x] land
  - [x] goto (lat, lon, alt)
  - [x] return to home
- [x] **1.4** Реализовать REST API:
  - [x] `POST /drone/arm`, `POST /drone/disarm`
  - [x] `POST /drone/takeoff` — body: `{ "altitude": 10 }`
  - [x] `POST /drone/land`
  - [x] `POST /drone/goto` — body: `{ "lat", "lon", "alt" }`
  - [x] `POST /drone/return-home`
- [x] **1.5** Проверить команды через API (например, Postman) при запущенном PX4+Gazebo

---

## Фаза 2: Телеметрия

- [x] **2.1** Реализовать **Telemetry Service**: приём данных от PX4 по MAVLink
- [x] **2.2** Собирать и отдавать: координаты (lat, lon, alt), скорость, заряд батареи, статус
- [x] **2.3** API для фронта: `GET /telemetry` или WebSocket с потоком телеметрии
- [x] **2.4** Формат ответа как в ТЗ: `{ "lat", "lon", "alt", "battery", "status" }`

---

## Фаза 3: Mission Engine

- [x] **3.1** Модель миссии: массив действий (takeoff, goto, wait, land и т.д.)
- [x] **3.2** Движок выполнения миссий по шагам (последовательно вызывать Drone Control + ждать завершения)
- [ ] **3.3** Поддержка условий и реакция на события (MVP — базовая версия)
- [x] **3.4** API: `POST /mission` — принять миссию (JSON из ТЗ), запустить выполнение
- [x] **3.5** API: статус миссии (текущий шаг, выполнено/в процессе/ошибка)

---

## Фаза 4: Navigation Module (MVP)

- [x] **4.1** Движение по waypoint (через goto)
- [x] **4.2** Простая логика маршрута: последовательность точек → последовательность goto

---

## Фаза 5: Frontend

- [x] **5.1** Инициализировать проект **React**
- [x] **5.2** Подключить карту (**Mapbox** или **Leaflet**)
- [x] **5.3** Настроить запросы к Backend API (axios/fetch) и при необходимости WebSocket для телеметрии

### Экраны

- [x] **5.4 Dashboard**
  - Статус дрона (из телеметрии)
  - Кнопки: Takeoff, Land, Return (вызов соответствующих API)
- [x] **5.5 Карта**
  - Отображение позиции дрона в реальном времени
  - Построение маршрута, отображение waypoints
- [x] **5.6 Mission Builder**
  - Создание миссии (список действий)
  - Drag & drop действий
  - Отправка миссии на сервер (`POST /mission`)

---

## Фаза 6: Интеграция и проверка

- [x] **6.1** Сценарий «Взлёт»: кнопка Takeoff → backend → MAVLink → PX4 → Gazebo
- [x] **6.2** Сценарий «Миссия»: создание миссии во фронте → выполнение через Mission Engine
- [x] **6.3** Сценарий «Телеметрия»: PX4 → backend → frontend (обновление статуса и карты)
- [x] **6.4** README: как запустить симуляцию, backend, frontend

---

## Не в MVP (расширения потом)

- Obstacle avoidance  
- Computer vision  
- Multi-drone  
- AI planner  
- Интеграция со складскими роботами  

---

**С чего начать сегодня:** Фаза 0 → Фаза 1 (до 1.5) → Фаза 5.1–5.4 (минимальный UI с кнопками и статусом).
